# === 핵심 프레임워크 (필수) ===
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# === 파일 처리 (필수) ===
openpyxl>=3.1.0          # Excel 파일 읽기/쓰기 및 스타일링
xlrd>=2.0.1              # 구버전 Excel 파일 (.xls) 읽기

# === 로깅 시스템 (장기 안정성 핵심) ===
loguru>=0.7.0            # 구조화된 로깅 시스템

# === 데이터 검증 (장기 안정성 핵심) ===
pandera>=0.17.0          # 데이터프레임 스키마 검증 및 품질 관리

# === 진행률 표시 (UX 개선) ===
tqdm>=4.65.0             # 기본 진행률 바
stqdm>=0.0.5             # Streamlit 전용 진행률 바

# === 성능 최적화 (대용량 데이터 처리) ===
bottleneck>=1.3.7        # pandas 연산 최적화 (30-50% 성능 향상)

# === 시각화 (데이터 분석) ===
plotly>=5.15.0           # 인터랙티브 차트 및 그래프

# === 문자열 매칭 (컬럼명 자동 감지) ===
fuzzywuzzy>=0.18.0       # 문자열 유사도 매칭
python-levenshtein>=0.21.0  # fuzzywuzzy 성능 향상

# === 날짜/시간 처리 (구매데이터 파싱) ===
python-dateutil>=2.8.0   # 다양한 날짜 형식 자동 파싱

# === 타입 힌트 지원 (코드 안정성) ===
typing-extensions>=4.7.0  # Python 3.8 미만에서 고급 타입 힌트 지원

# === 파일 경로 처리 (크로스 플랫폼 호환성) ===
pathlib2>=2.3.7          # Python 3.8 미만에서 향상된 경로 처리

# === 추가 최적화 (선택사항) ===
# numba>=0.57.0           # 수치 연산 가속화 (대용량 BOM 계산시)
# numexpr>=2.8.4          # 수식 계산 가속화
# joblib>=1.3.0           # 병렬 처리 지원

# === 메모리 관리 (선택사항) ===
# psutil>=5.9.0           # 메모리 사용량 모니터링
# memory-profiler>=0.61.0 # 메모리 프로파일링

# === 추가 파일 형식 지원 (선택사항) ===
# xlsxwriter>=3.1.0       # 대안 Excel 엔진
# pyxlsb>=1.0.10          # .xlsb 파일 지원

# === 보안 강화 (프로덕션 환경) ===
# cryptography>=41.0.0    # 파일 암호화
# python-dotenv>=1.0.0    # 환경변수 보안 관리

# === 데이터베이스 연결 (기업용 확장) ===
# sqlalchemy>=2.0.0       # SQL 데이터베이스 ORM
# pymysql>=1.1.0          # MySQL 연결
# psycopg2-binary>=2.9.0  # PostgreSQL 연결

# === 클라우드 스토리지 (기업용 확장) ===
# boto3>=1.28.0           # AWS S3 연결
# google-cloud-storage>=2.10.0  # Google Cloud Storage
# azure-storage-blob>=12.17.0   # Azure Blob Storage

# === 테스트 프레임워크 (개발용) ===
# pytest>=7.4.0
# pytest-cov>=4.1.0
# pytest-mock>=3.11.0



